#!/usr/bin/env ruby

require 'touch_this_test_that/client'

commit = ARGV.last

test_tool_call_options = ARGV[0, ARGV.size - 1]

client = TouchThisTestThat::Client.new(
  commit: commit,
  test_tool_call_options: test_tool_call_options,
  verbose: true)

client.call
